#include<stdio.h>
int main(){
    int arr[]={100,200,300,400};
    int n=sizeof(arr)/sizeof(arr[0]);
    int i=2;
    int newval=900;
    if(i>=0 && i<n){
        arr[i]=newval;
        printf("Updated array is ");
    for(i=0;i<n;i++)
    printf("%d ",arr[i]);
        }
   }
